<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="member">
	<sql id="search">
		<where>
			<choose>
				<when test="search == 1">
					id=#{keyword}
				</when>
			</choose>
		</where>
	</sql>

	<insert id="add">
		INSERT INTO member
		VALUES(#{id}, PASSWORD(#{password}), #{address}, #{name}, #{tel}, 0)
	</insert>
	<select id="item" resultType="Member">
		SELECT *
		FROM member
		WHERE id=#{id}
	</select>
	<select id="login" resultType="Member">
		SELECT *
		FROM member
		WHERE id=#{id}
		AND password=PASSWORD(#{password})
	</select>
	<select id="total" resultType="Integer">
		SELECT COUNT(*)
		FROM member
		<include refid="search" />
	</select>
	<select id="list" resultType="Member">
		SELECT *
		FROM member
		<include refid="search" />
		LIMIT #{offset}, #{perPage}
	</select>
	<delete id="delete">
		DELETE FROM member
		WHERE id=#{id}
	</delete>
	<update id="update">
		UPDATE member
		SET
		address=#{address},
		name=#{name},
		tel=#{tel},
		grade=#{grade}
		WHERE id=#{id}
	</update>
</mapper>