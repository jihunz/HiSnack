<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="product">
	<sql id="search">
		<where>
			
		</where>
	</sql>
	
	<resultMap type="Product" id="ProductMap">
		<id column="code" property="code"/>
		<result column="name" property="name"/>
		<result column="price" property="price"/>
		<result column="manufacture" property="manufacture"/>
		<collection property="images" column="image_code" ofType="Image" javaType="ArrayList">
			<id column="image_code" property="code"/>
			<result column="target" property="target"/>
			<result column="filename" property="filename"/>
			<result column="uuid" property="uuid"/>
		</collection>
	</resultMap>

	<insert id="add">
		INSERT INTO product
		VALUES(NULL, #{name}, #{price}, #{manufacture})
		
		<selectKey keyProperty="code" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	<select id="item" resultMap="ProductMap">
		SELECT product.*, product_image.code AS image_code, target, filename, uuid
		FROM product
		LEFT JOIN product_image ON product_image.target=product.code
		WHERE product.code=#{code}
	</select>
	<select id="total" resultType="Integer">
		SELECT COUNT(*)
		FROM product
		<include refid="search" />
	</select>
	<select id="list" resultType="Product">
		SELECT *
		FROM product
		<include refid="search" />
		LIMIT #{offset}, #{perPage}
	</select>
	<delete id="delete">
		DELETE FROM product
		WHERE code=#{code}
	</delete>
	<update id="update">
		UPDATE product
		SET
		name=#{name},
		price=#{price},
		manufacture=#{manufacture}
		WHERE code=#{code}
	</update>
</mapper>